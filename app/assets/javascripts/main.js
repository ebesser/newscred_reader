$(window).load(function() {      //Do the code in the {}s when the window has loaded

	var t = 2;
	setTimeout(function(){
		// $("#loader").fadeOut("fast");  //Show NewsCred screen for a t seconds
		// $("#lists").fadeIn("fast");
	}, t * 1000);

	console.log('pandas');
});
